package com.bll.lnkstudy.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.bll.lnkstudy.mvp.model.homework.CorrectDetailsBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CORRECT_DETAILS_BEAN".
*/
public class CorrectDetailsBeanDao extends AbstractDao<CorrectDetailsBean, Long> {

    public static final String TABLENAME = "CORRECT_DETAILS_BEAN";

    /**
     * Properties of entity CorrectDetailsBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property UserId = new Property(1, long.class, "userId", false, "USER_ID");
        public final static Property Type = new Property(2, int.class, "type", false, "TYPE");
        public final static Property Course = new Property(3, String.class, "course", false, "COURSE");
        public final static Property TypeStr = new Property(4, String.class, "typeStr", false, "TYPE_STR");
        public final static Property Title = new Property(5, String.class, "title", false, "TITLE");
        public final static Property Date = new Property(6, long.class, "date", false, "DATE");
        public final static Property Url = new Property(7, String.class, "url", false, "URL");
        public final static Property CorrectMode = new Property(8, int.class, "correctMode", false, "CORRECT_MODE");
        public final static Property ScoreMode = new Property(9, int.class, "scoreMode", false, "SCORE_MODE");
        public final static Property Score = new Property(10, int.class, "score", false, "SCORE");
        public final static Property CorrectJson = new Property(11, String.class, "correctJson", false, "CORRECT_JSON");
        public final static Property AnswerUrl = new Property(12, String.class, "answerUrl", false, "ANSWER_URL");
    }


    public CorrectDetailsBeanDao(DaoConfig config) {
        super(config);
    }
    
    public CorrectDetailsBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CORRECT_DETAILS_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE ," + // 0: id
                "\"USER_ID\" INTEGER NOT NULL ," + // 1: userId
                "\"TYPE\" INTEGER NOT NULL ," + // 2: type
                "\"COURSE\" TEXT," + // 3: course
                "\"TYPE_STR\" TEXT," + // 4: typeStr
                "\"TITLE\" TEXT," + // 5: title
                "\"DATE\" INTEGER NOT NULL ," + // 6: date
                "\"URL\" TEXT," + // 7: url
                "\"CORRECT_MODE\" INTEGER NOT NULL ," + // 8: correctMode
                "\"SCORE_MODE\" INTEGER NOT NULL ," + // 9: scoreMode
                "\"SCORE\" INTEGER NOT NULL ," + // 10: score
                "\"CORRECT_JSON\" TEXT," + // 11: correctJson
                "\"ANSWER_URL\" TEXT);"); // 12: answerUrl
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CORRECT_DETAILS_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CorrectDetailsBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getUserId());
        stmt.bindLong(3, entity.getType());
 
        String course = entity.getCourse();
        if (course != null) {
            stmt.bindString(4, course);
        }
 
        String typeStr = entity.getTypeStr();
        if (typeStr != null) {
            stmt.bindString(5, typeStr);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(6, title);
        }
        stmt.bindLong(7, entity.getDate());
 
        String url = entity.getUrl();
        if (url != null) {
            stmt.bindString(8, url);
        }
        stmt.bindLong(9, entity.getCorrectMode());
        stmt.bindLong(10, entity.getScoreMode());
        stmt.bindLong(11, entity.getScore());
 
        String correctJson = entity.getCorrectJson();
        if (correctJson != null) {
            stmt.bindString(12, correctJson);
        }
 
        String answerUrl = entity.getAnswerUrl();
        if (answerUrl != null) {
            stmt.bindString(13, answerUrl);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CorrectDetailsBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getUserId());
        stmt.bindLong(3, entity.getType());
 
        String course = entity.getCourse();
        if (course != null) {
            stmt.bindString(4, course);
        }
 
        String typeStr = entity.getTypeStr();
        if (typeStr != null) {
            stmt.bindString(5, typeStr);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(6, title);
        }
        stmt.bindLong(7, entity.getDate());
 
        String url = entity.getUrl();
        if (url != null) {
            stmt.bindString(8, url);
        }
        stmt.bindLong(9, entity.getCorrectMode());
        stmt.bindLong(10, entity.getScoreMode());
        stmt.bindLong(11, entity.getScore());
 
        String correctJson = entity.getCorrectJson();
        if (correctJson != null) {
            stmt.bindString(12, correctJson);
        }
 
        String answerUrl = entity.getAnswerUrl();
        if (answerUrl != null) {
            stmt.bindString(13, answerUrl);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CorrectDetailsBean readEntity(Cursor cursor, int offset) {
        CorrectDetailsBean entity = new CorrectDetailsBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // userId
            cursor.getInt(offset + 2), // type
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // course
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // typeStr
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // title
            cursor.getLong(offset + 6), // date
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7), // url
            cursor.getInt(offset + 8), // correctMode
            cursor.getInt(offset + 9), // scoreMode
            cursor.getInt(offset + 10), // score
            cursor.isNull(offset + 11) ? null : cursor.getString(offset + 11), // correctJson
            cursor.isNull(offset + 12) ? null : cursor.getString(offset + 12) // answerUrl
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CorrectDetailsBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUserId(cursor.getLong(offset + 1));
        entity.setType(cursor.getInt(offset + 2));
        entity.setCourse(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setTypeStr(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setTitle(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setDate(cursor.getLong(offset + 6));
        entity.setUrl(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
        entity.setCorrectMode(cursor.getInt(offset + 8));
        entity.setScoreMode(cursor.getInt(offset + 9));
        entity.setScore(cursor.getInt(offset + 10));
        entity.setCorrectJson(cursor.isNull(offset + 11) ? null : cursor.getString(offset + 11));
        entity.setAnswerUrl(cursor.isNull(offset + 12) ? null : cursor.getString(offset + 12));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CorrectDetailsBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CorrectDetailsBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CorrectDetailsBean entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
