package com.bll.lnkstudy.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.bll.lnkstudy.mvp.model.PaintingTypeBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PAINTING_TYPE_BEAN".
*/
public class PaintingTypeBeanDao extends AbstractDao<PaintingTypeBean, Long> {

    public static final String TABLENAME = "PAINTING_TYPE_BEAN";

    /**
     * Properties of entity PaintingTypeBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property UserId = new Property(1, long.class, "userId", false, "USER_ID");
        public final static Property Type = new Property(2, int.class, "type", false, "TYPE");
        public final static Property Grade = new Property(3, int.class, "grade", false, "GRADE");
        public final static Property Date = new Property(4, long.class, "date", false, "DATE");
        public final static Property IsCloud = new Property(5, boolean.class, "isCloud", false, "IS_CLOUD");
        public final static Property CloudId = new Property(6, int.class, "cloudId", false, "CLOUD_ID");
    }


    public PaintingTypeBeanDao(DaoConfig config) {
        super(config);
    }
    
    public PaintingTypeBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PAINTING_TYPE_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE ," + // 0: id
                "\"USER_ID\" INTEGER NOT NULL ," + // 1: userId
                "\"TYPE\" INTEGER NOT NULL ," + // 2: type
                "\"GRADE\" INTEGER NOT NULL ," + // 3: grade
                "\"DATE\" INTEGER NOT NULL ," + // 4: date
                "\"IS_CLOUD\" INTEGER NOT NULL ," + // 5: isCloud
                "\"CLOUD_ID\" INTEGER NOT NULL );"); // 6: cloudId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PAINTING_TYPE_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, PaintingTypeBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getUserId());
        stmt.bindLong(3, entity.getType());
        stmt.bindLong(4, entity.getGrade());
        stmt.bindLong(5, entity.getDate());
        stmt.bindLong(6, entity.getIsCloud() ? 1L: 0L);
        stmt.bindLong(7, entity.getCloudId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, PaintingTypeBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getUserId());
        stmt.bindLong(3, entity.getType());
        stmt.bindLong(4, entity.getGrade());
        stmt.bindLong(5, entity.getDate());
        stmt.bindLong(6, entity.getIsCloud() ? 1L: 0L);
        stmt.bindLong(7, entity.getCloudId());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public PaintingTypeBean readEntity(Cursor cursor, int offset) {
        PaintingTypeBean entity = new PaintingTypeBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // userId
            cursor.getInt(offset + 2), // type
            cursor.getInt(offset + 3), // grade
            cursor.getLong(offset + 4), // date
            cursor.getShort(offset + 5) != 0, // isCloud
            cursor.getInt(offset + 6) // cloudId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, PaintingTypeBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUserId(cursor.getLong(offset + 1));
        entity.setType(cursor.getInt(offset + 2));
        entity.setGrade(cursor.getInt(offset + 3));
        entity.setDate(cursor.getLong(offset + 4));
        entity.setIsCloud(cursor.getShort(offset + 5) != 0);
        entity.setCloudId(cursor.getInt(offset + 6));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(PaintingTypeBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(PaintingTypeBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(PaintingTypeBean entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
